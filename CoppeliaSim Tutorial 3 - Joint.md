# CoppeliaSim Tutorial 3
## ジョイント（関節）と階層構造：振り子を作る

これまで「置いて転がす」だけでしたが、今回は**「パーツをつないで動かす」**ことに挑戦します。
これができれば、ロボットアームや車、そして最終目標である**人体モデル**を作る基礎が完成します。

---

## 🎯 目的
- **Revolute Joint（回転関節）** の使い方を覚える
- **Scene Hierarchy（階層構造）** の重要性を理解する（親と子の関係）
- **Static**（固定）と **Dynamic**（可動）の使い分けを実践する

---

## 🛠 使用環境
- CoppeliaSim（Bullet 物理エンジン）

---

## 1. 新しいシーンを作成
1. **File → New scene**

---

## 2. 土台（Stand）の作成
まずは関節を支える「土台」を作ります。これは動かないようにします。

### 2.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. 名前を `Stand` に変更（Scene hierarchy でダブルクリック）
3. サイズを設定：
   - X = 0.1
   - Y = 0.1
   - Z = 0.5（柱のような形）

### 2.2 位置を設定
1. **Position** 設定：
   - Z = 0.25（床に立つように）

### 2.3 動的プロパティ（固定）
1. **Dynamic properties**：
   - Body is dynamic: ❌ **OFF**（固定するため）
   - Body is respondable: ✅ ON

---

## 3. ジョイント（Joint）の作成
これが回転の軸になります。

### 3.1 Joint を追加
1. **Add → Joint → Revolute**
2. Scene hierarchy に `Revolute_joint` が作成されます。

### 3.2 位置と向きを設定
1. **Position** 設定：
   - X = 0.1（柱の横に配置）
   - Y = 0
   - Z = 0.45（柱の上の方）
1. **Rotation ダイアログ**を開く（左上ツールバーの回転アイコン：矢印が回っているマーク）
2. **Orientation** タブで設定：
   - **Alpha = 90**（横向き：Y軸周りの回転にする）
   - Beta = 0
   - Gamma = 0

> 💡 **ポイント**: ジョイントの太い円柱部分が回転軸です。これが横を向くようにします。

### 3.3 ジョイントのモード設定（重要！）
1. Scene hierarchy で `Revolute_joint` をダブルクリックして設定ダイアログを開く
2. **Scene Object Properties → Joint** タブ
3. **Mode** を確認：
   - **Dynamic mode** に設定されていることを確認（これが物理演算モードです）
   - ※ 古いバージョンでは "Torque/force mode" と表記されることがあります。
4. **詳細設定を確認**：
   - 同画面下部の **Dynamic properties dialog** ボタンをクリック
   - **Control mode** が **Free** になっていることを確認
   - （※古いバージョンや設定によっては "Motor enabled" というチェックボックスの場合がありますが、FreeモードならOKです）

---

## 4. 振り子（Link）の作成
ぶら下がる棒の部分です。

### 4.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. 名前を `Link` に変更（Scene hierarchy で名前をダブルクリック）
3. サイズを設定：
   - X = 0.4（長さ）
   - Y = 0.05
   - Z = 0.05

### 4.2 位置を設定
1. **Position** 設定：
   - **X = 0.35**（ジョイントから少し離す：接触防止のため）
   - Y = 0
   - Z = 0.45（ジョイントと同じ高さ）

### 4.3 動的プロパティ（可動）
1. **Dynamic properties**：
   - Body is dynamic: ✅ **ON**（動く！）
   - Body is respondable: ✅ ON

---

## 5. 階層構造（Hierarchy）を作る 【最重要】
ここが**一番のポイント**です。CoppeliaSimでは、**「親」が動くと「子」もついてくる**というルールがあります。
関節機能を持たせるには、以下の順番でつなぐ必要があります。

**正しい構造：**
`Stand` (固定親)
  └── `Revolute_joint` (関節)
        └── `Link` (動く子)

### 手順
Scene hierarchy ウィンドウでドラッグ＆ドロップ操作を行います。

1. `Link` をドラッグして、`Revolute_joint` の上でドロップ
   → これで Link が Joint の「子」になります。
2. `Revolute_joint` をドラッグして、`Stand` の上でドロップ
   → これで Joint が Stand の「子」になります。

> ⚠️ **確認**: `Stand` の左にある `+` や `>` をクリックして展開したとき、上記のような階層ツリーになっていますか？

---

## 6. パラメータ最終確認（重要）

うまく動かない場合は、以下の数値と完全に一致しているか確認してください。
（Position / Rotation は **World（絶対座標）** での値です）

| オブジェクト | Position (X, Y, Z) | Orientation (Alpha, Beta, Gamma) | 備考                     |
| ------------ | ------------------ | -------------------------------- | ------------------------ |
| **Stand**    | `0.00, 0.00, 0.25` | `0, 0, 0`                        | Body is dynamic: **OFF** |
| **Joint**    | `0.10, 0.00, 0.45` | `90, 0, 0`                       | Alpha=90 (Y軸回転)       |
| **Link**     | `0.35, 0.00, 0.45` | `0, 0, 0`                        | X=0.35 (少し離す)        |

---

## 7. シミュレーション実行

1. ツールバーの ⏱️ **Toggle real time mode** を**ON**にする（時計アイコンが青くなっていればOK）
2. ▶️ **Start simulation**
3. `Link` が重力でパタンと下がり、ブランブランと揺れれば成功！振り子の完成です。

> 💡 **ポイント**: Real time mode を ON にすると、シミュレーションが実時間に合わせて進行します。OFF だと可能な限り高速に計算されるため、動きが速すぎて確認しづらくなることがあります。

---

## 🔧 トラブルシューティング

| 症状                              | 原因                              | 解決策                                        |
| --------------------------------- | --------------------------------- | --------------------------------------------- |
| **全部落ちてしまう**              | Stand が Dynamic になっている     | Stand の Body is dynamic を **OFF** にする    |
| **Link が落ちてしまう（外れる）** | 階層構造が間違っている            | `Joint` の下に `Link` があるか確認する        |
| **動かない（固まっている）**      | Joint のモードがおかしい          | Joint の Mode を **Torque/force mode** にする |
| **変な方向に回る**                | Joint の向き（Orientation）が違う | 回転軸（太い円柱）の向きを確認する            |

---

## 💡 Tips：オブジェクトのサイズを後から変更する

作成済みのオブジェクト（Cuboid など）のサイズを変更したい場合：

1. 変更したいオブジェクト（例: `Stand`）をダブルクリック
2. **Scene object properties** ダイアログが開く
3. **Geometry** ボタンをクリック
4. **Size** の X, Y, Z 値を編集

> ⚠️ **注意**: シミュレーション実行中は変更できません。停止してから編集してください。

---

## 8. 解説：なぜ離れていても繋がっているの？

シミュレーションで「あれ？」と思ったかもしれません。
**「Stand と Joint、Joint と Link は隙間があるのに、なぜ落ちずに繋がっているの？」**

### 🔗 階層構造（Hierarchy）＝ 見えないアーム
CoppeliaSimにおける**親子関係（階層構造）**は、**物理的な剛体接続**を意味します。
親と子の間に隙間があっても、そこには**「質量ゼロの透明で絶対に折れない棒」**があると考えてください。

### 🏢 Stand（土台）の役割
1.  **アンカー（錨）効果**:
    *   Stand は `Body is dynamic: OFF`（静的）に設定しました。
    *   これは「宇宙空間に釘で打ち付けられて絶対に動かない」状態です。
2.  **連鎖の固定**:
    *   Stand が動かないため、その「子」である Joint も（Standに対する相対位置が）固定されます。
    *   もし Stand がなければ、Joint も Link も重力に従って無限に落下してしまいます。

この仕組みのおかげで、ロボットの腕や足を作るときに、複雑なジョイント接合部をモデリングしなくても、「位置を合わせるだけ」で関節として機能するのです。

---

## 🚀 次へのステップ
この「ジョイント構造」が理解できれば、次は**モーター**を使って自力で動かすことができます。
（例：振り子をプロペラのように回す、車のタイヤを回すなど）
