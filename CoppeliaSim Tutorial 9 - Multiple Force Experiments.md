# CoppeliaSim Tutorial 9
## 複数の外力を指定のタイミングで加える実験

Tutorial 8 では単発の外力を骨盤に加えましたが、このチュートリアルでは「右から押された後、左から押される」といった連続的な外力や、異なる部位への外力印加をプログラムする方法を学びます。

---

## 🎯 目的
- 複数の外力イベント（タイミング、大きさ、方向）をスクリプトで管理する
- テーブル（リスト）を使った効率的なイベント処理の実装
- 複雑な外力に対するモデルの応答を観察する

---

## 🛠 使用環境
- Tutorial 8 で作成した `SimpleBiped_Exp.ttt` を使用します（またはコピーして使用）。

---

## 1. スクリプトの拡張方針

これまでは `forceDelay`, `forceMagnitude` といった単一の変数で管理していましたが、これでは複数のイベントを扱うのが大変です。
そこで、**「イベントリスト」** を作成して、順番に処理していく方式に変更します。

### イベントリストの構造例
```lua
forceEvents = {
    {time=2.0, duration=0.3, force={0, 5.0, 0}, pos={0, 0, 0}},   -- 1回目: 右へ
    {time=4.0, duration=0.3, force={0, -5.0, 0}, pos={0, 0, 0}},  -- 2回目: 左へ
    {time=6.0, duration=0.5, force={10.0, 0, 0}, pos={0, 0, 0.1}} -- 3回目: 前へ（上部）
}
```

---

## 2. 実装手順（予定）

1. Tutorial 8 のスクリプトを `sysCall_init` と `sysCall_actuation` 全体を書き換える
2. ループ処理を使って、現在の時間にマッチするイベントがないか毎フレーム確認する
3. マッチしたイベントがあれば `sim.addForce` を実行する

(詳細はユーザーの指示に従い記述していきます)
