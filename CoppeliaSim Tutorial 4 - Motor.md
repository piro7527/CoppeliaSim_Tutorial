# CoppeliaSim Tutorial 4
## モーターで回す：振り子からプロペラへ

Tutorial 3で作った「振り子」は重力で揺れるだけでしたが、今回は**モーターの力**を使って自力で回転させてみます。
これがロボットの「アクチュエータ（駆動力）」の基本になります。

---

## 🎯 目的
- **Control mode** を理解し、Jointをモーターとして動作させる
- **Target velocity（目標速度）** と **Max torque（最大トルク）** の関係を学ぶ

---

## 🛠 使用環境
- CoppeliaSim（Bullet 物理エンジン）
- **前提**: Tutorial 3 の「振り子」が完成していること

---

## 1. シーンの準備
Tutorial 3 で作成したシーン（Stand + Joint + Link）をそのまま使います。
もし閉じてしまった場合は、もう一度 Tutorial 3 の手順で振り子を作ってください。

---

## 2. Joint をモーター化する
「自由回転（Free）」から「モーター駆動（Force）」に設定を変更します。

### 2.1 設定ダイアログを開く
1. Scene hierarchy で **`Revolute_joint`** を選択
2. **Dynamic properties dialog** を開く（ツールバーまたはプロパティ画面の下部から）

### 2.2 Control mode を変更
1. **Control mode** を変更：
   - 変更前: `Free`
   - 変更後: **`Velocity`**
   
   > 💡 **重要**: ここでは **Velocity**（速度制御）を選んでください。画像の "Force" だと「一定の力」しか設定できません。

### 2.3 モーター設定を入力
1. **Velocity** を選択すると、以下の項目が表示されます：
   - **Target velocity**: `50` (deg/s) --- 1秒間に50度回転
   - **Max torque**: `100` (N·m) --- 非常に強い力
   - （※ Motor enabled というチェックボックスはなく、モード選択だけでOKです）

---

## 3. シミュレーション実行（高速回転）

1. ▶️ **Start simulation**
2. 振り子（Link）がウィーンと回転し始めるはずです！
   （重力に逆らって持ち上がり、プロペラのように回ります）

---

## 4. 実験：トルク（力）の限界を知る

モーターの力が弱すぎると、重いものを持ち上げられません。これを体験してみましょう。

1. **Max torque** を変更してみる：
   - `100` → **`0.1`** (N·m) に変更
2. ▶️ **Start simulation**
   - どうなりましたか？
   - おそらく、「頑張って持ち上げようとするけれど、重さに負けて持ち上がらない（プルプルする）」動きになるはずです。

> 🤖 **ロボット工学のポイント**:
> ロボットを設計するときは、持ち上げたいアームや荷物の重さに応じて、十分なトルクのあるモーター（サーボ）を選ぶ必要があります。シミュレータなら「数値を書き換えるだけ」で最強のモーターが手に入ります！

---

## 🔧 トラブルシューティング

| 症状                               | チェック項目                                                                         |
| ---------------------------------- | ------------------------------------------------------------------------------------ |
| **回らない（ブランブランのまま）** | Control mode が `Free` のままになっていませんか？ `Force` に変更してください。       |
| **回らない（少し動くけど落ちる）** | `Target velocity` が 0 になっていませんか？ または `Max torque` が小さすぎませんか？ |
| **逆回転する**                     | `Target velocity` をマイナス（例: `-50`）にすると逆回転します。                      |

---

## 🚀 次へのステップ
これで「関節を動かす」ことができるようになりました。
これを組み合わせれば、**車（タイヤを回す）** や **ロボットアーム（肘を曲げる）** が作れます！

次は、ロボットの「目」である **センサー（Sensor）** を使って、物体を検知する方法を学びましょう。
