# CoppeliaSim Tutorial 10
## 立ち上がり介助実験：モデル作成と制御

CoppeliaSimのチュートリアル第10弾です。今回は**椅子からの立ち上がり動作**（Sit-to-Stand）のシミュレーションを作成します。
骨盤・大腿・下腿・足部からなる4リンクモデルを作成し、椅子に座った状態から関節トルクを制御して立ち上がる動作を実現します。

---

## 🎯 目的
- **立ち上がり用モデル**（座った姿勢）を作成する
- **椅子と環境**をセットアップする
- **関節制御**により、椅子から立ち上がる動作を実現する
- 重心移動と床反力の関係を理解する

---

## 🛠 使用環境
- CoppeliaSim（Bullet 物理エンジン）
- **前提**: Tutorial 1〜7程度の知識があること

---

## 1. シーンと環境の作成

まずは椅子と床を作成します。

### 1.1 新しいシーンの作成
1. **File** > **New scene** で新しいシーンを作成します。

### 1.2 床（Floor）の確認
シーンに既に `Floor` がある場合はそのままでOKです。ない場合は以下で作成します（通常はデフォルトで `ResizableFloor` などがあります）。
もしなければ：
1. **Add** > **Primitive shape** > **Plane**
2. サイズを適当に（例: 5m x 5m）設定して作成し、`Floor` と名前を付けます。
3. **Body is respondable** をONにします。
4. **Body is dynamic** はOFF（固定）にします。

### 1.3 椅子（Chair）の作成
座面と背もたれを持つシンプルな椅子を作成します。

**座面（Seat）:**
1. **Add** > **Primitive shape** > **Cuboid**
2. サイズ: **X=0.4, Y=0.4, Z=0.05**
3. 位置 (Position): **Z = 0.375** （高さ40cmの椅子の座面中心）
   - ※ 座面上面 = 0.375 + 0.025 = **0.40**
4. 名前を `ChairSeat` に変更
5. **Static** に設定（**Body is dynamic** のチェックを**外す**）
6. **Respondable** は **ON**

**脚（ChairLegs） - 省略可:**
今回は簡単のため、座面が空中に浮いている状態（Staticなので落ちない）でも実験は可能です。見た目を気にする場合は脚を追加してください。

---

## 2. 人体モデルの作成（座位姿勢）

椅子に座った状態のモデルを作成します。
モデル構成：**Pelvis** (骨盤), **Thigh** (大腿), **Shank** (下腿), **Foot** (足部)
左右対称に作成します。

モデルは **X軸方向** を向いて座る設定にします。

### 2.1 骨盤（Pelvis）
1. **Add** > **Primitive shape** > **Cuboid**
2. サイズ: **X=0.15, Y=0.25, Z=0.15**
3. 質量 (Mass): **2.0 kg**
4. 位置: **X=0, Y=0, Z=0.475** （座面の上に配置）
   - ※ 座面の上面(Z=0.40) + 骨盤の高さ半分(0.075) = 0.475
5. 名前: `Pelvis`
6. **Dynamic** & **Respondable**

### 2.2 右脚の作成

#### 右股関節（RHipJoint）
1. **Add** > **Joint** > **Revolute**
2. 名前: `RHipJoint`
3. 位置: **X=0, Y=-0.08, Z=0.44** （骨盤の右下寄り）
   - ※ 座面(0.40) + 大腿半径(0.04) = 0.44
   - ※ 座標系の注意: X軸を「前」とすると、Y軸は「左」になります（右手系）。そのため、**右**側は **Yマイナス** になります。
4. **Orientation**: **Alpha=-90, Beta=0, Gamma=0** (Y軸回転＝矢状面での屈伸)
   - ※ モデルがX軸方向を向いているため、屈伸軸はY軸になります。
5. 親: `Pelvis`
6. 初期角度: 座った状態にするため、**90度** 曲げたいところですが、初期姿勢はモデル作成時の配置で決まります。
   - CoppeliaSimでは、ジョイントの0度位置＝現在の姿勢 となります。
   - なので、まずは「座った形」でオブジェクトを配置していきます。

#### 右大腿（RThigh）
1. **Add** > **Primitive shape** > **Cylinder**
2. サイズ: **半径 0.04, 長さ 0.35**
3. 質量: **1.5 kg**
4. 名前: `RThigh`
5. 位置: **X=0.175, Y=-0.08, Z=0.44** （股関節から前方へ）
   - ※ 長さ0.35mの中心（0.175m）に配置することで、股関節(X=0)と膝関節(X=0.35)の間をぴったり繋ぎます。
6. **Orientation**: **Alpha=0, Beta=90, Gamma=0** （横倒し＝水平）
7. 親: `RHipJoint` ← **Position/Orientationを設定した後に親を設定**

#### 右膝関節（RKneeJoint）
1. **Add** > **Joint** > **Revolute**
2. 名前: `RKneeJoint`
3. 位置: **X=0.35, Y=-0.08, Z=0.44** （大腿の先端）
4. **Orientation**: **Alpha=-90, Beta=0, Gamma=0**
5. 親: `RThigh`

#### 右下腿（RShank）
1. **Add** > **Primitive shape** > **Cylinder**
2. サイズ: **半径 0.035, 長さ 0.40** （※椅子の高さに合わせて長さを変更）
3. 質量: **1.0 kg**
4. 名前: `RShank`
5. 位置: **X=0.35, Y=-0.08, Z=0.24** （膝から下方へ）
   - ※ 膝(Z=0.44)から足首(Z=0.04)の間の中心 = (0.44+0.04)/2 = 0.24
6. **Orientation**: **Alpha=0, Beta=0, Gamma=0** （垂直）
7. 親: `RKneeJoint`

#### 右足首関節（RAnkleJoint）
1. **Add** > **Joint** > **Revolute**
2. 名前: `RAnkleJoint`
3. 位置: **X=0.35, Y=-0.08, Z=0.04** （下腿の下端）
   - ※ 足部の上面(Z=0.04)に配置
4. **Orientation**: **Alpha=-90, Beta=0, Gamma=0**
5. 親: `RShank`

#### 右足部（RFoot）
1. **Add** > **Primitive shape** > **Cuboid**
2. サイズ: **X=0.18, Y=0.08, Z=0.04**
3. 質量: **0.4 kg**
4. 名前: `RFoot`
5. 位置: **X=0.40, Y=-0.08, Z=0.02** （床の上に接地）
6. 親: `RAnkleJoint`
7. 色: 分かりやすくするため、少し色を変えておくと良いでしょう。

> ⚠️ **重要**: 各オブジェクトは、**Position と Orientation を設定してから親を設定**してください。親を先に設定すると、座標がローカル座標系に変換されて意図しない位置になることがあります。

### 2.3 左脚の作成（コピー）
1. Scene hierarchyで `RHipJoint` から `RFoot` までを**全て選択**します。
   - ※ **Shiftキー（またはCtrl/Cmdキー）を押しながら**、階層内の全てのオブジェクトを **一つずつクリック** して選択してください。
2. **Ctrl+C** でコピーし、**Ctrl+V** でペーストします。
3. ペーストしたオブジェクトの名前を `LHipJoint` ... `LFoot` に変更します。
4. Y座標の符号を反転させます（例: Y=-0.08 → **Y=0.08**）。
   - `LHipJoint`: Y=0.08
   - `LThigh`: Y=0.08
   - `LKneeJoint`: Y=0.08
   - ...

### 2.4 モデルの確認
- 椅子に座り、足が床ついている状態になっていますか？
- 階層構造は以下のようになっているはずです：
```
Pelvis
 ├── RHipJoint -> RThigh -> RKneeJoint -> RShank -> RAnkleJoint -> RFoot
 └── LHipJoint -> LThigh -> LKneeJoint -> LShank -> LAnkleJoint -> LFoot
```

### 2.5 座標の確認表（重要）
画像で隙間ができている場合は、以下の値を再確認してください（すべてWorld座標系）：

| オブジェクト    | X     | Y     | Z (高さ)  | 備考                              |
| --------------- | ----- | ----- | --------- | --------------------------------- |
| **Pelvis**      | 0.0   | 0.0   | **0.475** | 座面の上                          |
| **RHipJoint**   | 0.0   | -0.08 | **0.44**  |                                   |
| **RThigh**      | 0.175 | -0.08 | **0.44**  | 水平                              |
| **RKneeJoint**  | 0.35  | -0.08 | **0.44**  |                                   |
| **RShank**      | 0.35  | -0.08 | **0.24**  | **長さが 0.40m** であることを確認 |
| **RAnkleJoint** | 0.35  | -0.08 | **0.04**  | 足の上                            |
| **RFoot**       | 0.40  | -0.08 | **0.02**  | 床の上                            |

> **ヒント**: 親子関係を作った後に座標がずれた場合は、Scene hierarchyで一度オブジェクトを親から切り離し（ドラッグして空の場所にドロップ）、再配置してから数値を入力し直してください。

---

## 3. ジョイントの制御モード設定

立ち上がり動作のため、各関節をトルク制御または位置制御できるようにします。
今回はシンプルに **位置制御 (Position control)** で目標角度を与えて動かします（高トルクで無理やり動かすイメージ）。より高度な実験では Force mode でトルク制御を行いますが、まずは動きを作ることを優先します。

1. 全てのジョイント (`RHipJoint`〜`LAnkleJoint`) を選択します。
2. **Double click** > **Dynamics dialog** を開きます。
3. **Control loop enabled** にチェックを入れます。
4. **Spring/damper mode** にチェックされている場合、クリックして設定画面へ。
   - **Position control (PID)** にチェックが入っていることを確認。
   - **Proportional (P)**: 1.0 などのデフォルト値でOK（弱ければ後で強くします）。
   - **Max. torque/force**: **100.0** 程度に増やしておきます（デフォルト2.5だと体重を支えきれない可能性があります）。

---

## 4. 立ち上がりスクリプトの作成

Pelvis にスクリプトを追加して、各関節を動かします。

1. `Pelvis` を選択 > **Add** > **Script** > **Child script (Non-threaded)**
2. 以下のLuaコードを記述します。

```lua
function sysCall_init()
    -- ハンドルの取得
    rHip = sim.getObjectHandle("RHipJoint")
    lHip = sim.getObjectHandle("LHipJoint")
    rKnee = sim.getObjectHandle("RKneeJoint")
    lKnee = sim.getObjectHandle("LKneeJoint")
    rAnkle = sim.getObjectHandle("RAnkleJoint")
    lAnkle = sim.getObjectHandle("LAnkleJoint")

    -- 初期状態（座位）の角度を現在値として保持（あるいは0度定義）
    -- 今回のモデル作成手順では、座った状態が「0度」になっています。
    -- 立ち上がるには：
    -- 股関節：伸展（マイナス方向へ回転、またはプラス方向。軸の向きによる）
    -- 膝関節：伸展（同上）
    -- 足首：底背屈調整

    -- 軸の向き確認（右脚）：
    -- Hip: Y軸回転。Thighが下がる方向へ回したい → 時計回り？
    -- Knee: Y軸回転。Shankが上がる（伸びる）方向へ回したい。
    
    -- 動作フェーズ
    phase = 0
    timer = 0
end

function sysCall_actuation()
    dt = sim.getSimulationTimeStep()
    timer = timer + dt
    
    -- ターゲット角度の設定（単位：ラジアン）
    -- 立ち上がり：股関節と膝関節を伸ばす（0度 = 座位直角 と仮定した場合、伸展は -90度 = -1.57rad 方向？）
    -- ※ 実際の回転方向はシーン上のJointのZ軸矢印を確認してください。
    -- ここでは試行錯誤的に値を設定します。
    
    -- フェーズ1: 重心前方移動（お辞儀） [0.0s - 1.0s]
    if timer < 1.0 then
        -- 股関節を深く曲げる（上体を前に倒す）
        targetHip = 45 * math.pi / 180 
        targetKnee = 0
        targetAnkle = 20 * math.pi / 180 -- 足首を少し背屈
        
    -- フェーズ2: 離殿・伸展（立ち上がり） [1.0s - 3.0s]
    elseif timer < 3.0 then
        -- 股関節・膝関節を伸展（直立へ）
        -- 座位が0度（直角）なら、直立は -90度（股・膝とも）になるはず（モデル構成による）
        
        -- 現在の幾何学的配置（座標配置）でジョイントを作成した場合、
        -- ジョイント角0 = 現在の姿勢（座った姿勢）になります。
        -- したがって、直立するには：
        -- 股関節：大腿に対して骨盤を起こす（伸展） → -90度 (-1.57) 
        -- 膝関節：大腿に対して下腿を伸ばす（伸展） → 0度（既に90度曲がっている状態を0とするなら…あ、いや違う）
        
        -- 【重要】今の作り方だと：
        -- Hip: 親(Pelvis)に対し子(Thigh)は水平。直立時Thighは垂直。Relで-90度。
        -- Knee: 親(Thigh)に対し子(Shank)は垂直。直立時Shankは垂直。Relで+90度。
        
        targetHip = -10 * math.pi / 180  -- 最終的に直立（骨盤が立つ）
        targetKnee = 90 * math.pi / 180  -- 膝が伸びる（はず）
        targetAnkle = 10 * math.pi / 180 -- バランス調整
        
    else
        -- 姿勢保持
    end
    
    -- 制御実行（P制御）
    sim.setJointTargetPosition(rHip, targetHip)
    sim.setJointTargetPosition(lHip, targetHip)
    sim.setJointTargetPosition(rKnee, targetKnee)
    sim.setJointTargetPosition(lKnee, targetKnee)
    sim.setJointTargetPosition(rAnkle, targetAnkle)
    sim.setJointTargetPosition(lAnkle, targetAnkle)
end
```

> **⚠️ 注意:** 回転方向（プラスかマイナスか）は、ジョイントの設置向きによって異なります。
> - シミュレーション実行時、足が変な方向に曲がったら、`target...` の符号（プラスマイナス）を逆にしてください。
> - 「お辞儀」のフェーズがないと、重心が後ろに残ったまま膝を伸ばそうとして、後ろに転倒します。

---

## 5. 実験と調整

### 5.1 とりあえず実行
**Play** ボタンを押して動きを確認します。
- **後ろに倒れる場合**: お辞儀（重心前方移動）が足りないか、足の位置が前すぎます。
- **崩れ落ちる場合**: ジョイントの最大トルク（Max torque）が足りない可能性があります。関節設定で `200` や `500` に増やしてみてください。

### 5.2 重心の確認
重心位置を可視化することで、なぜ倒れるかが分かります。
グラフ機能などを使ってCOM（Center of Mass）を表示するのも良い練習ですが、まずは目視で「お尻が浮く瞬間に、重心が足の上にあるか」を確認しましょう。

---

## 6. 課題
1. **スムーズな立ち上がり**: パラメータを調整して、転倒せずに綺麗に立ち上がるシーケンスを作ってください。
2. **座面の高さ**: 椅子の高さを変えると、立ち上がりの難易度はどう変わりますか？
3. **介助**: もし自力で立ち上がれない（トルク制限を弱くするなど）場合、外部からどのような力を加えれば立てるでしょうか？（Tutorial 9の応用）

---

次回は、このモデルを使って実際に「介助プロトコル」を検証していきます。
