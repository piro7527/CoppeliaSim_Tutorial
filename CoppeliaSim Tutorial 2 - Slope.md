# CoppeliaSim Tutorial 2
## Sphere をスロープで転がしてゴールに当てる

このチュートリアルでは、**斜面（スロープ）を使ってSphereを転がし、ゴールに当てる**シミュレーションを作成します。

---

## 🎯 目的
- 静的オブジェクト（スロープ、ゴール）と動的オブジェクト（Sphere）の配置を学ぶ
- オブジェクトの**回転（Orientation）**設定を理解する
- 位置の計算（めり込み防止）を実践する

---

## 1. 新しいシーンを作成
1. CoppeliaSim を起動
2. **File → New scene**

---

## 2. 床（Floor）の確認

### 床の重要性について

CoppeliaSim のデフォルトの**グラウンドプレーン**（チェッカー模様の地面）は**視覚的な参照線のみ**で、**物理的な衝突判定がありません**。

そのため、Sphere が地面を**すり抜けて無限に落下**しないように、物理的な床（respondable なオブジェクト）が必要です。

このチュートリアルでは、**既存の "Floor" オブジェクト**を使用します。

---

### 2.1 Floor オブジェクトの確認
1. **Scene hierarchy**（左側のツリー表示）を確認します。
2. **Floor** という名前のオブジェクトが存在することを確認してください。

> 💡 **Tip**: もし Floor がない場合は、**Add → Primitive shape → Cuboid** から作成してください（サイズ: X=5.0, Y=5.0, Z=0.05 / 位置: Z=0.025）。

### 2.2 動的プロパティの確認
床が正しく物理判定を持つか確認します。

1. Scene hierarchy で **Floor** を選択
2. **Shape → Dynamic properties dialog** を開く
3. 以下の設定になっているか確認：

| 項目                | 設定  |
| ------------------- | ----- |
| Body is dynamic     | ❌ OFF |
| Body is respondable | ✅ ON  |

> **Body is respondable** が ON でないと、物体は床をすり抜けてしまいます。

---

## 3. スロープの作成

### 3.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. サイズを設定：
   - X = 0.5（長さ）
   - Y = 0.3（幅）
   - Z = 0.02（厚さ：薄い板）
3. 作成された **Cuboid** をダブルクリックして、名前を **Slope** に変更します。

> 💡 **Info**: 作成直後は **Body is dynamic** にチェックが入っている場合がありますが、**ステップ 3.4** で設定を変更します。現時点ではそのままで構いません。

### 3.2 位置を設定
1. Position ダイアログを開く（左上ツールバーの位置アイコン）
2. **Position** タブで設定：
   - X = 0
   - Y = 0
   - Z = 0.20

### 3.3 回転（傾き）を設定
1. **Rotation ダイアログ**を開く（左上ツールバーの回転アイコン）
2. **Orientation** タブで設定：
   - Alpha = 0
   - **Beta = +30**（Y軸周りに傾ける）
   - Gamma = 0

> 💡 **ポイント**: Beta の符号でスロープの傾き方向が変わります。ゴールに向かって下り坂になるように調整してください。

### 3.4 動的プロパティを設定
| 項目                | 設定                          |
| ------------------- | ----------------------------- |
| Body is dynamic     | ❌ **OFF**（静的オブジェクト） |
| Body is respondable | ✅ ON                          |

> ⚠️ **重要**: Body is dynamic を ON にすると、シミュレーション開始時にスロープが落下してしまいます。

---

## 4. ゴール（受け皿）の作成

### 4.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. サイズを設定：
   - X = 0.15
   - Y = 0.15
   - Z = 0.05
3. 作成された **Cuboid** をダブルクリックして、名前を **Goal** に変更します。

### 4.2 位置を設定
- X = 0.35（スロープの下端付近）
- Y = 0
- Z = 0.05（床の上）

> 💡 **Info**: 床の高さによっては、ゴールが少し浮いて見えることがありますが、**Body is dynamic** が OFF（静的）なので落下しません。そのままでシミュレーションに支障はありません。

### 4.3 動的プロパティを設定
| 項目                | 設定  |
| ------------------- | ----- |
| Body is dynamic     | ❌ OFF |
| Body is respondable | ✅ ON  |

---

## 5. Sphere（ボール）の作成

### 5.1 Sphere を追加
1. **Add → Primitive shape → Sphere**
2. 半径 = 0.03（小さめ）

### 5.2 位置を設定
スロープの上端に配置：
- X = -0.10
- Y = 0
- Z = 0.28（スロープ表面に近い位置）

> 💡 **ポイント**: Sphere がスロープの表面に接するか、わずかに上になるように配置します。
>
> **補足**: ほんの少しめり込んでいても、シミュレーション開始時に物理エンジンが自動的に押し出してくれるので**問題ありません**。もし開始直後に Sphere が大きく跳ねてしまう場合は、Z を **0.29** や **0.30** に少し上げてみてください。

### 5.3 動的プロパティを設定
| 項目                | 設定                         |
| ------------------- | ---------------------------- |
| Body is dynamic     | ✅ **ON**（動的オブジェクト） |
| Body is respondable | ✅ ON                         |

### 5.4 物理プロパティを設定（オプション）
1. Sphere をダブルクリックして **Scene Object Properties** を開く
2. **Show dynamic properties dialog** ボタンをクリック
3. 開いたダイアログの **Engine properties** ボタンをクリック
4. 表示されるJSONで `bullet` セクションの値を編集：
```json
"restitution": 0.3,
"friction": 0.5
```

---

## 6. シミュレーション実行

## 6. シミュレーション実行

1. ⏱️ ツールバーの **Toggle real-time mode**（ウサギとカメのアイコン、または時計のアイコン）をクリックして **ON** にします。
   - これを忘れると、計算が速すぎて一瞬で終わってしまいます。
2. ▶️ **Start simulation** ボタンをクリック
3. Sphere がスロープを転がり落ちるのを確認
4. ゴールに当たって止まれば成功！

---

## 📹 シミュレーションの録画方法

### macOS
**Shift + Command + 5** → 画面収録

### Windows
**Windows + Alt + R**（Xbox Game Bar）

### CoppeliaSim 内蔵機能
**Tools → Video recorder**

---

## 🔧 トラブルシューティング

| 問題                                | 原因                           | 解決策                                |
| ----------------------------------- | ------------------------------ | ------------------------------------- |
| スロープが落下する                  | Body is dynamic が ON          | OFF に変更                            |
| Sphere が跳ねてスロープを転がらない | 初期位置がスロープから離れすぎ | Z位置を調整してスロープ表面に近づける |
| Sphere がゴールと反対方向に転がる   | スロープの傾きが逆             | Beta の符号を変更（+30 ↔ -30）        |
| オブジェクトがめり込む              | Z位置が低すぎる                | Z = 厚さ/2 に設定                     |

---

## 📋 オブジェクト設定まとめ

| オブジェクト      | Body is dynamic | Body is respondable |
| ----------------- | --------------- | ------------------- |
| Floor（床）       | ❌ OFF           | ✅ ON                |
| Slope（スロープ） | ❌ OFF           | ✅ ON                |
| Goal（ゴール）    | ❌ OFF           | ✅ ON                |
| Sphere（ボール）  | ✅ **ON**        | ✅ ON                |

---

## 🚀 発展課題

1. **スロープの角度を変えて**転がる速度の違いを観察
2. **摩擦（friction）を変えて**滑りやすさを比較
3. **ゴールを箱型にして**Sphere を中に入れる
4. **複数の Sphere を転がして**衝突を観察
