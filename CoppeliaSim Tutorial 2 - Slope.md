# CoppeliaSim Tutorial 2
## Sphere をスロープで転がしてゴールに当てる

このチュートリアルでは、**斜面（スロープ）を使ってSphereを転がし、ゴールに当てる**シミュレーションを作成します。

---

## 🎯 目的
- 静的オブジェクト（スロープ、ゴール）と動的オブジェクト（Sphere）の配置を学ぶ
- オブジェクトの**回転（Orientation）**設定を理解する
- 位置の計算（めり込み防止）を実践する

---

## 🛠 使用環境
- CoppeliaSim（Bullet 物理エンジン）
- OS：Windows / macOS / Linux

---

## 1. 新しいシーンを作成
1. CoppeliaSim を起動
2. **File → New scene**

---

## 2. 床（Floor）の作成

### なぜ床を作成するのか？

CoppeliaSim のデフォルトの**グラウンドプレーン**（チェッカー模様の地面）は**視覚的な参照線のみ**で、**物理的な衝突判定がありません**。

床（respondable なオブジェクト）がないと、Sphere は地面を**すり抜けて無限に落下**してしまいます。

> 💡 **注意**: シーンに既に **"Floor" オブジェクト**がある場合（Scene hierarchy で確認）、この手順はスキップできます。

---

### 2.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. サイズを設定：
   - X = 2.0
   - Y = 2.0
   - Z = 0.05

### 2.2 位置を設定
1. オブジェクトを選択
2. **Tools → Scene Object Properties → Object タブ**
3. Position を設定：
   - X = 0, Y = 0, **Z = 0.025**（厚さの半分）

> ⚠️ **注意**: Z=0 にすると床がグラウンドプレーンにめり込みます。**Z = 厚さ/2** に設定してください。

### 2.3 動的プロパティを設定
1. **Shape → Dynamic properties dialog** を開く
2. 設定：

| 項目                | 設定  |
| ------------------- | ----- |
| Body is dynamic     | ❌ OFF |
| Body is respondable | ✅ ON  |

3. **Apply to selection**

---

## 3. スロープの作成

### 3.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. サイズを設定：
   - X = 0.5（長さ）
   - Y = 0.3（幅）
   - Z = 0.02（厚さ：薄い板）

### 3.2 位置を設定
1. Position ダイアログを開く（左上ツールバーの位置アイコン）
2. **Position** タブで設定：
   - X = 0
   - Y = 0
   - Z = 0.20

### 3.3 回転（傾き）を設定
1. **Rotation ダイアログ**を開く（左上ツールバーの回転アイコン）
2. **Orientation** タブで設定：
   - Alpha = 0
   - **Beta = +30**（Y軸周りに傾ける）
   - Gamma = 0

> 💡 **ポイント**: Beta の符号でスロープの傾き方向が変わります。ゴールに向かって下り坂になるように調整してください。

### 3.4 動的プロパティを設定
| 項目                | 設定                          |
| ------------------- | ----------------------------- |
| Body is dynamic     | ❌ **OFF**（静的オブジェクト） |
| Body is respondable | ✅ ON                          |

> ⚠️ **重要**: Body is dynamic を ON にすると、シミュレーション開始時にスロープが落下してしまいます。

---

## 4. ゴール（受け皿）の作成

### 4.1 Cuboid を追加
1. **Add → Primitive shape → Cuboid**
2. サイズを設定：
   - X = 0.15
   - Y = 0.15
   - Z = 0.05

### 4.2 位置を設定
- X = 0.35（スロープの下端付近）
- Y = 0
- Z = 0.05（床の上）

### 4.3 動的プロパティを設定
| 項目                | 設定  |
| ------------------- | ----- |
| Body is dynamic     | ❌ OFF |
| Body is respondable | ✅ ON  |

---

## 5. Sphere（ボール）の作成

### 5.1 Sphere を追加
1. **Add → Primitive shape → Sphere**
2. 半径 = 0.03（小さめ）

### 5.2 位置を設定
スロープの上端に配置：
- X = -0.10
- Y = 0
- Z = 0.28（スロープ表面に近い位置）

> 💡 **ポイント**: Sphere がスロープの表面に接するか、わずかに上になるように配置します。離れすぎると落下時に跳ねてしまいます。

### 5.3 動的プロパティを設定
| 項目                | 設定                         |
| ------------------- | ---------------------------- |
| Body is dynamic     | ✅ **ON**（動的オブジェクト） |
| Body is respondable | ✅ ON                         |

### 5.4 物理プロパティを設定（オプション）
**Engine properties** → Bullet：
```json
"restitution": 0.3,
"friction": 0.5
```

---

## 6. シミュレーション実行

1. ▶️ **Start simulation** ボタンをクリック
2. Sphere がスロープを転がり落ちるのを確認
3. ゴールに当たって止まれば成功！

---

## 📹 シミュレーションの録画方法

### macOS
**Shift + Command + 5** → 画面収録

### Windows
**Windows + Alt + R**（Xbox Game Bar）

### CoppeliaSim 内蔵機能
**Tools → Video recorder**

---

## 🔧 トラブルシューティング

| 問題                                | 原因                           | 解決策                                |
| ----------------------------------- | ------------------------------ | ------------------------------------- |
| スロープが落下する                  | Body is dynamic が ON          | OFF に変更                            |
| Sphere が跳ねてスロープを転がらない | 初期位置がスロープから離れすぎ | Z位置を調整してスロープ表面に近づける |
| Sphere がゴールと反対方向に転がる   | スロープの傾きが逆             | Beta の符号を変更（+30 ↔ -30）        |
| オブジェクトがめり込む              | Z位置が低すぎる                | Z = 厚さ/2 に設定                     |

---

## 📋 オブジェクト設定まとめ

| オブジェクト      | Body is dynamic | Body is respondable |
| ----------------- | --------------- | ------------------- |
| Floor（床）       | ❌ OFF           | ✅ ON                |
| Slope（スロープ） | ❌ OFF           | ✅ ON                |
| Goal（ゴール）    | ❌ OFF           | ✅ ON                |
| Sphere（ボール）  | ✅ **ON**        | ✅ ON                |

---

## 🚀 発展課題

1. **スロープの角度を変えて**転がる速度の違いを観察
2. **摩擦（friction）を変えて**滑りやすさを比較
3. **ゴールを箱型にして**Sphere を中に入れる
4. **複数の Sphere を転がして**衝突を観察
